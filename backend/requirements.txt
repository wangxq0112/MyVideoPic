# MyVideoPic 后端依赖
#
# 全部为纯本地运行所需，无任何联网 / 遥测组件。
# 安装：pip install -r requirements.txt
#
# 另需两个外部可执行文件（不通过 pip 安装，须在 PATH 中）：
#   ffmpeg  —— 视频截帧生成封面
#   ffprobe —— 读取时长 / 分辨率 / 编码格式
# 缺失时视频仍可入库，只是没有封面且无法判定浏览器兼容性。

Django==5.1.5
djangorestframework==3.15.2
Pillow==11.1.0
